# 音源目录（用户自备，不随仓库分发）

本目录用于存放**用户自己导入的音源脚本**，默认不提交任何第三方音源。

- 支持 `.js` 音源脚本或落雪音乐导出的 `user_api.json`
- 可直接把音源文件放进本目录，也可在 GUI「音源」页导入（文件会被复制到 `%APPDATA%\lx-downloader-sources\`）
- 引擎默认从 `%APPDATA%\lx-downloader-sources\` 加载音源；本目录仅作为仓库占位与说明

> 请自行确认你使用的音源脚本的授权与合规性。
